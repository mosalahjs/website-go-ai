import BootDataClient from "./components/CompanyDataClient";

export const metadata = {
  title: "Company Data Management",
};

export default function BootDataPage() {
  return <BootDataClient />;
}
